{
  "name": "@feryardiant/firebase",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "functions",
    "website"
  ],
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "build": "pnpm run --parallel --if-present build",
    "deploy": "firebase deploy",
    "emulator": "firebase emulators:start",
    "firebase": "cross-env GOOGLE_APPLICATION_CREDENTIALS=.firebase/service-account.json firebase",
    "lint": "pnpm run --parallel --if-present lint",
    "lint:fix": "pnpm run --parallel --if-present lint:fix",
    "prepare": "husky install .vscode/husky",
    "publish:packages": "lerna publish from-package --ignore-scripts",
    "serve": "run-p emulator watch",
    "watch": "pnpm run --parallel --if-present watch"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/config-conventional": "^12.1.4",
    "cross-env": "^7.0.3",
    "dotenv": "^10.0.0",
    "eslint": "~7.30.0",
    "firebase-tools": "^9.16.0",
    "husky": "^7.0.1",
    "lerna": "~4.0.0",
    "lint-staged": "^11.0.0",
    "npm-run-all": "^4.1.5",
    "prettier": "~2.3.2"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "lint-staged": {
    "{functions,websites}/src/**/*.{js,vue}": [
      "eslint --fix"
    ]
  }
}
